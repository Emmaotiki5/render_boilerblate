<%= form_with(model: expert, class: "form mx-auto") do |form|  %>
  <% if expert.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expert.errors.count, "error") %> prohibited this expert from being saved:</h2>

      <ul>
        <% expert.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block", class:"form-label" %>
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div>
    <%= form.label :department, style: "display: block", class:"form-label" %>
    <%= form.text_field :department, class:"form-control" %>
  </div>
  <%= render 'image_preview', form: form, post: expert%>

  <div>
    <%= form.submit  class:"btn btn-secondary mt-5" %>
  </div>
<% end %>
